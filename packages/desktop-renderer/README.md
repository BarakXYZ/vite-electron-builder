# @app/desktop-renderer

The desktop renderer package is the React + Vite frontend for the Electron app.

## Development

```bash
pnpm --filter @app/desktop-renderer dev
```

## Build

```bash
pnpm --filter @app/desktop-renderer build
```
